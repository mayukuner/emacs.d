#include <algorithm>
#include <queue>
#include <cstdio>
#include <cstring>
#include <vector>
#include <string>
#include <cmath>
#include <map>
#include <set>
#include <queue>
#define FOR(i, n) for (int i = 1; i <= n; i++)
#define REP(i, n) for (int i = 0; i < n; i++)

using namespace std;

int main() {
#ifdef QWERTIER
  freopen("in.txt", "r", stdin);
#endif
  return 0;
}
